[project]
name = "ai-english"
version = "0.1.0"
description = "Streamlit 单词记忆应用"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.40.0",
]

[project.scripts]
dev = "streamlit run src/app.py"
